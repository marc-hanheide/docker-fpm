install: |
  echo "Hello, world!"
  touch /etc/FOO
  echo "Goodbye."
preinstall: |
  echo "This is a preinstall script"
  echo "Goodbye."
package: foo
maintainer: "Marc Hanheide <marc@hanheide.net>"
version: 0.0.2
dependencies:
  #- build-essential